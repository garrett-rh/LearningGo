package integers

//Takes two integers and returns their sum
func Add(x, y int) int { //Same as Add(x int, y int) however, if both vars are of the same type, go will assume that the last type matches the first
	return x + y
}
